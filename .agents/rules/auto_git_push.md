# Auto Git Push Rule

After every successful set of changes, feature additions, or bug fixes:
1. Verify the code builds cleanly and relevant tests pass.
2. Stage modified files and commit with a clean, descriptive semantic commit message.
3. Automatically push the commit to the GitHub remote repository (`origin main`).
4. Report the commit hash and sync status to the user.
